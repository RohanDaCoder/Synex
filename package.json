{
	"name": "synex",
	"module": "index.ts",
	"private": true,
	"scripts": {
		"start": "bun dist/index.js",
		"dev": "bun --watch run src/",
		"build": "bun run format && tsc && bun build src/index.ts --sourcemap --minify --outdir=dist/ --target=bun",
		"build:node": "bun run build --target=node",
		"deploy": "bun run build && bun run start",
		"format": "oxfmt"
	},
	"type": "module",
	"dependencies": {
		"axios": "^1.13.2",
		"discord.js": "^14.25.1",
		"dotenv": "^17.2.3",
		"parse-duration": "^2.1.4",
		"yoctocolors": "^2.1.2"
	},
	"devDependencies": {
		"oxfmt": "^0.15.0",
		"typescript": "^5.9.3"
	}
}
